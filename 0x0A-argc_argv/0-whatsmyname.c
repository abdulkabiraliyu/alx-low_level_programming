#include <stdio.h>
#include "main.h"


/**
* main - Entry poit to the program
* @argc: stores the number of arg passed from command line
* @argv: stores the arg passed from the command line
*
* Return: Always 0 (Success)
*/

int main(int argc __attribute__ ((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);

}
