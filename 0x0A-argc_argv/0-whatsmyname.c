#include <stdio.h>
/**
 * main - an entry into the program
 *
 * @argc: number of command line argument supplied
 * @argv: array of string of command line argument
 *
 * Return: 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);

}
